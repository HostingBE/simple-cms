{% include 'header.twig' %}

<div class="d-flex justify-content-center">
    <div class="row pt-5 w-50">

        <h1 class="pt-5 text-white">{{ title | capitalize }}</h1>    

        {% include 'menu.twig' %}

    <div class="col-md-9 bg-white">
   Welkom bij de permissies controleer de output {{ name }}

{% if directories|length < 1 %}
no directories or files to checkbox
{% endif %}

{% if directories|length > 0 %}

<dl class="row">
{% for directory in directories %}
   
    <dt class="col-6">{{ directory.name }} ({{ directory.type }})</dt>
    <dd class="col-6"></dd>     
    {% endfor %}
</dl>
    {% endif %}
    </div>
{% include 'urls.twig' %}
</div>
</div>

{% include 'footer.twig' %}